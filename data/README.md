# Data

This directory is reserved for local datasets and dataset inputs that are intentionally kept separate from application code. Do not add generated evaluation output or credentials here.

The current dataset format and synthetic repository workflow are documented in `docs/eval/dataset_manifest.md` and `docs/WORKFLOW_M3_TO_M4.md`.
