# Copy to .env and fill in your external MySQL connection values. Never commit .env.
# Alternatively, set DATABASE_URL to a complete mysql+pymysql URL.
DB_HOST=mysql.example.com
DB_PORT=3306
DB_USER=your_mysql_user
DB_PASS=your_mysql_password
DB_NAME=shd_walker
